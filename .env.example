DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pure_veg_cafe"
JWT_SECRET="replace-with-a-32-byte-random-secret"
SUPER_ADMIN_EMAIL="owner@example.com"
D_TREAT_OWNER_PASSWORD_HASH="$2a$12$replace-with-a-bcrypt-hash"
SUPER_ADMIN_PASSWORD_HASH="$2a$12$replace-with-bcrypt-hash"
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="smtp-user"
SMTP_PASS="smtp-pass"
SMTP_FROM="Pure Veg Cafe <orders@example.com>"
NEXT_PUBLIC_APP_URL="https://your-hosted-domain.com"
# Create and authorize this voice in HeyGen using public/assets/dtreat-voice.ogg.
# When unset, menu narration uses the visitor device's built-in voice instead.
HEYGEN_API_KEY=""
HEYGEN_VOICE_ID=""
